import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container : {
        marginRight : 25
    },
    badge: {
        position: 'absolute',
        top: -8,
        right: -8,
        fontSize: 14
    }
})

export default styles;